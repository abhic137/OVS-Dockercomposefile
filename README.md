# OVS-Dockercomposefile
## To start
sudo docker-compose up -d
## To check the process status
sudo docker ps
## To get into the vswitchd bash to execute commands
sudo docker exec -it ovs-vswitchd bash
